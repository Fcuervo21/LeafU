import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    body: {
        margin: 0
    },
    worldCard: {
        overflow: 'hidden',
        width: '100%',
        height: '100%',
        background: '#ff0000'
    }
}));
