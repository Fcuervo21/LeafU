import { Card } from '@mui/material';
import React from 'react';
import useStyles from './styles'

const classes = useStyles();

const InfoCard = (type, color) => {
    <Card className="classes.">

    </Card>
}

export default InfoCard;